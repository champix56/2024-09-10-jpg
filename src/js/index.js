
/**
 * initialisation pour events d'index
 * @param {string} a context du doc
 * @param {number} b hnbdfg
 * @param {*} c nhdryjdtyjddt
 */
function initialisation(a,b,c) {
    var a = document.querySelector('#header .js');
    a.addEventListener('click',function (evt) {
    })
}

initialisation(null,rf,fuk)

//document.addEventListener('DOMContentLoaded',initialisation);